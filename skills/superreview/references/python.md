# Python

Check shared mutable function defaults, truthiness versus valid zero/empty values,
timezone boundaries, resource cleanup, exception paths, and async cancellation.
Follow event-loop blocking and unbounded fan-out to a concrete workload. Check framework
semantics before reporting a mutable model-field default as shared state.
Inspect validation boundaries, coercion, generators consumed twice, and runtime-supported
type syntax. Do not impose a formatter, import style, or clock abstraction the project does
not use. Tests should exercise behavior, failure paths, and previous valid inputs.
